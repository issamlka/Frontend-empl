export interface EmployeeInterface {
    id: number;
    firstName: string;
    lastName: string;
    email: string;
    address?: string; // Optional
    phone?: string; // Optional
    salaries?: number; // Optional
  }
  